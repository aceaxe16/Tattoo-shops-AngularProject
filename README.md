# Tattoo-shops-AngularProject

To start the project:
1. Navigate to the server folder with the command cd server.Then start the server with node server.
2. In a different terminal navigate to the tatttoo-shops-project folder with the command cd tatttoo-shops-project. Then run ng serve --open to open the app in your browser.
